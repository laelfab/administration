"use client";
import { DEFAULT_REDIRECT_USER_LOGGED } from "./all-route";

interface RedirectToProps {
  role: number;
}
export const AuthRedirectTo = ({ role }: RedirectToProps) => {
  if (role !== 2) {
  }
};
